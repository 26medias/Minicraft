// The ONLY definition site (engine home: render and game import it; engine never imports game).
/** Chebyshev rings in chunks around the player's chunk (spec header). Never derive one from another. */
// 6/7/8 (was 5/6/7): the parent found fog at 72 blocks too close; fog far = MESH_RADIUS*16-8 = 88.
export const MESH_RADIUS = 6;
export const UNMOUNT_RADIUS = 7;
export const DATA_RADIUS = 8;
